import Logo from '../icons/logo.svg';

export default () => (
    <div>
        <Logo />

        <style global jsx>{`
            svg {
                float: right;
                fill: #FD746C;
            }
        `}</style>
    </div>
);
